---
# tasks file for processing a single client

- name: Compute client email
  ansible.builtin.set_fact:
    x_ui_inbounds_client_email: >-
      {{
        client_item.name + '-' +
        (inbound_item.name + client_item.name) | hash('md5') | truncate(8, end='')
      }}

- name: Check if client exists by email
  ansible.builtin.set_fact:
    x_ui_inbounds_existing_client: "{{ existing_clients_by_email[x_ui_inbounds_client_email] | default(None) }}"

- name: Build client payload object
  ansible.builtin.set_fact:
    x_ui_inbounds_client_payload_obj:
      id: "{{ generated_uuids[client_item.name] }}"
      email: "{{ x_ui_inbounds_client_email }}"
      flow: "{{ client_flow }}"
      enable: true

- name: Convert client payload to JSON string (for settings field)
  ansible.builtin.set_fact:
    x_ui_inbounds_client_payload_string: "{{ {'clients': [x_ui_inbounds_client_payload_obj]} | to_json }}"

- name: Add new client via API
  ansible.builtin.uri:
    url: "{{ ui_panel_url }}/panel/api/inbounds/addClient"
    method: POST
    headers:
      Cookie: "{{ session_cookie }}"
      Content-Type: "application/json"
    body:
      id: "{{ inbound_id }}"
      settings: "{{ x_ui_inbounds_client_payload_string }}"
    body_format: json
    status_code: 200
  when: x_ui_inbounds_existing_client is none
  register: x_ui_inbounds_add_client_response
  failed_when: >-
    x_ui_inbounds_add_client_response.json is not defined or
    x_ui_inbounds_add_client_response.json.success != true

- name: Update existing client via API
  ansible.builtin.uri:
    url: "{{ ui_panel_url }}/panel/api/inbounds/updateClient/{{ x_ui_inbounds_existing_client.id }}"
    method: POST
    headers:
      Cookie: "{{ session_cookie }}"
      Content-Type: "application/json"
    body:
      id: "{{ inbound_id }}"
      settings: "{{ x_ui_inbounds_client_payload_string }}"
    body_format: json
    status_code: 200
  when: x_ui_inbounds_existing_client is not none
  register: x_ui_inbounds_update_client_response
  failed_when: >-
    x_ui_inbounds_update_client_response.json is not defined or
    x_ui_inbounds_update_client_response.json.success != true
